import ProfileInfo from "@/components/ui/ProfileInfo";
import { Box } from "@mui/material";

const Profile = () => {

    return (
        <Box className="mt-8">
            <ProfileInfo />
        </Box>
    );
};

export default Profile;
